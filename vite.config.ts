// Re-export the frontend Vite configuration so tools that look for vite.config.ts
// at the project root can find it. The canonical config lives in frontend/vite.config.ts.
export { default } from './frontend/vite.config'
